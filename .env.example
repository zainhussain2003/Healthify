# Copy this file to .env and fill in your values
# .env is gitignored — never commit real secrets

# Database
DB_HOST=localhost
DB_PORT=5433
DB_NAME=healthify
DB_USER=healthify
DB_PASSWORD=Healthify2026

# JWT (generate a random 256-bit base64 string for production)
JWT_SECRET=changeme-use-a-real-256-bit-secret-in-production
JWT_EXPIRATION_MS=86400000

# AI (openai or gemini)
AI_PROVIDER=openai
AI_API_KEY=sk-...
AI_MODEL=gpt-4o-mini
